[package]
name=aws-resource-list
version=1.0.0
description=Scan an AWS account for any resources.
license=MIT

[dependencies]
arg=*
parallel=*
string=*
strict=*

[install]
aws-resource-list=aws-resource-list
